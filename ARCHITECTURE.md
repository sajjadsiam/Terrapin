# Terrapin Attack - Architecture Diagram

## Network Topology

```
┌─────────────────────────────────────────────────────────────────┐
│                      Docker Network                              │
│                    terrapin-net (172.20.0.0/24)                 │
│                                                                  │
│                                                                  │
│  ┌───────────────────┐                                          │
│  │   SSH Client      │                                          │
│  │  172.20.0.30      │                                          │
│  │                   │                                          │
│  │  • Test Client    │                                          │
│  │  • Python Script  │                                          │
│  └─────────┬─────────┘                                          │
│            │                                                     │
│            │ SSH Connection                                      │
│            │ (Can connect directly OR through attacker)         │
│            │                                                     │
│            ▼                                                     │
│  ┌───────────────────┐                                          │
│  │   Attacker (MITM) │                                          │
│  │  172.20.0.20      │                                          │
│  │  Port: 2222       │                                          │
│  │                   │                                          │
│  │  • Attack Proxy   │                                          │
│  │  • Packet Drop    │◄────────────────────────┐               │
│  │  • Traffic Capture│                         │               │
│  │  • MITM Tools     │                         │               │
│  └─────────┬─────────┘                         │               │
│            │                                    │               │
│            │ Forwarded SSH                      │               │
│            │ (with packet manipulation)         │               │
│            │                                    │               │
│            ▼                                    │               │
│  ┌───────────────────┐                         │               │
│  │ Vulnerable Server │                         │               │
│  │  172.20.0.10      │                         │               │
│  │  Port: 22         │                         │               │
│  │                   │                         │               │
│  │  • OpenSSH        │                         │               │
│  │  • ChaCha20       │                         │               │
│  │  • ext-info       │                         │               │
│  └───────────────────┘                         │               │
│                                                 │               │
│  Host Machine Ports:                           │               │
│  • 2222 → vulnerable-server:22 (direct)        │               │
│  • 3333 → attacker:2222 (MITM proxy)           │               │
│                                                 │               │
└─────────────────────────────────────────────────┼───────────────┘
                                                  │
                                    Host Machine Port Access
```

## Attack Flow Diagram

```
Client                    Attacker (MITM)              Vulnerable Server
  │                             │                             │
  │  1. SSH Banner Exchange     │                             │
  ├─────────────────────────────┼─────────────────────────────>│
  │  SSH-2.0-Client_1.0         │                             │
  │                             │                             │
  │<────────────────────────────┼─────────────────────────────┤
  │                             │      SSH-2.0-OpenSSH_...    │
  │                             │                             │
  │  2. Key Exchange Init       │                             │
  ├─────────────────────────────┼─────────────────────────────>│
  │  SSH_MSG_KEXINIT            │         (forwarded)         │
  │  [ext-info-c]               │                             │
  │                             │                             │
  │<────────────────────────────┼─────────────────────────────┤
  │                             │      SSH_MSG_KEXINIT        │
  │                             │      [ext-info-s]           │
  │                             │                             │
  │  3. DH Key Exchange         │                             │
  ├─────────────────────────────┼─────────────────────────────>│
  │  SSH_MSG_KEX_ECDH_INIT      │                             │
  │                             │                             │
  │<────────────────────────────┼─────────────────────────────┤
  │                             │    SSH_MSG_KEX_ECDH_REPLY   │
  │                             │                             │
  │  4. New Keys Message        │                             │
  ├─────────────────────────────┼─────────────────────────────>│
  │  SSH_MSG_NEWKEYS            │                             │
  │  [Seq counter reset to 0]   │                             │
  │                             │                             │
  │<────────────────────────────┼─────────────────────────────┤
  │                             │      SSH_MSG_NEWKEYS        │
  │                             │      [Seq counter reset]    │
  │                             │                             │
  │  5. Extension Info          │                             │
  │<────────────────────────────X─────────────────────────────┤
  │                          DROPPED!     SSH_MSG_EXT_INFO    │
  │                            [❌]        [Seq 0]             │
  │                             │                             │
  │  6. Service Request         │                             │
  ├─────────────────────────────┼─────────────────────────────>│
  │  SSH_MSG_SERVICE_REQUEST    │    (Now appears as Seq 0    │
  │  [Should be Seq 1]          │     instead of Seq 1!)      │
  │                             │                             │
  │  🎯 ATTACK SUCCESSFUL       │                             │
  │  Extension info dropped     │                             │
  │  Security downgrade active  │                             │
  │                             │                             │
```

## Sequence Number Manipulation

### Normal Flow (No Attack)

```
After SSH_MSG_NEWKEYS:
┌──────────┬──────────────────────────┬──────────┐
│ Seq Num  │ Message Type             │ Status   │
├──────────┼──────────────────────────┼──────────┤
│    0     │ SSH_MSG_EXT_INFO         │ Sent     │
│    1     │ SSH_MSG_SERVICE_REQUEST  │ Sent     │
│    2     │ SSH_MSG_USERAUTH_REQUEST │ Sent     │
│   ...    │ ...                      │ ...      │
└──────────┴──────────────────────────┴──────────┘
```

### Terrapin Attack Flow

```
After SSH_MSG_NEWKEYS:
┌──────────┬──────────────────────────┬──────────────┐
│ Seq Num  │ Message Type             │ Status       │
├──────────┼──────────────────────────┼──────────────┤
│    0     │ SSH_MSG_EXT_INFO         │ ❌ DROPPED   │
│    0     │ SSH_MSG_SERVICE_REQUEST  │ ✓ Forwarded  │ ← Was Seq 1
│    1     │ SSH_MSG_USERAUTH_REQUEST │ ✓ Forwarded  │ ← Was Seq 2
│   ...    │ ...                      │ ...          │
└──────────┴──────────────────────────┴──────────────┘

Result: Server never realizes a packet was dropped!
```

## Container Architecture

```
┌─────────────────────────────────────────────────────────────┐
│ vulnerable-server container                                  │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ OpenSSH Server (sshd)                                   │ │
│ │ • Listening on port 22                                  │ │
│ │ • ChaCha20-Poly1305 enabled                            │ │
│ │ • ext-info-s advertised                                │ │
│ │ • Weak configuration (intentional)                     │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ attacker container                                           │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ Python 3 + Scapy                                        │ │
│ │ • terrapin_exploit.py (main attack script)             │ │
│ │ • packet_interceptor.py (packet manipulation)          │ │
│ │ • MITM proxy on port 2222                              │ │
│ │ • tcpdump for traffic capture                          │ │
│ │ • Analysis tools                                        │ │
│ └─────────────────────────────────────────────────────────┘ │
│ Capabilities: NET_ADMIN, NET_RAW, Privileged                │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ client container                                             │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ OpenSSH Client + Python                                 │ │
│ │ • test_client.py (automated SSH client)                │ │
│ │ • Paramiko library                                      │ │
│ │ • Can connect to either server directly or via MITM    │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

## Data Flow: Packet Interception

```
┌────────────┐
│ Client     │
│ Sends      │
│ Packet     │
└─────┬──────┘
      │
      ▼
┌─────────────────────────────────────────┐
│ Attacker Proxy (terrapin_exploit.py)    │
│                                          │
│  ┌────────────────────────────────────┐ │
│  │ 1. Receive packet from client      │ │
│  └───────────┬────────────────────────┘ │
│              ▼                           │
│  ┌────────────────────────────────────┐ │
│  │ 2. Parse packet (packet_interceptor)│ │
│  │    • Check packet type              │ │
│  │    • Decode SSH message             │ │
│  └───────────┬────────────────────────┘ │
│              ▼                           │
│  ┌────────────────────────────────────┐ │
│  │ 3. Decision: Forward or Drop?      │ │
│  │    • If NEWKEYS: note it, forward  │ │
│  │    • If EXT_INFO after NEWKEYS:    │ │
│  │      ❌ DROP (don't forward)       │ │
│  │    • Anything else: forward        │ │
│  └───────────┬────────────────────────┘ │
│              ▼                           │
│  ┌────────────────────────────────────┐ │
│  │ 4. Forward to server (if not dropped)│
│  └───────────┬────────────────────────┘ │
└──────────────┼──────────────────────────┘
               ▼
        ┌────────────┐
        │ Vulnerable │
        │ Server     │
        └────────────┘
```

## File Structure

```
terapin/
├── README.md                    # Main documentation
├── ATTACK_DETAILS.md           # Technical deep-dive
├── LAB_WALKTHROUGH.md          # Step-by-step guide
├── QUICKSTART.md               # Quick setup
├── LICENSE                      # Terms
├── .gitignore                  # Git ignore rules
├── docker-compose.yml          # Lab orchestration
├── setup.sh                    # Linux setup script
├── setup.bat                   # Windows setup script
│
├── poc_exploit/                # Proof-of-concept exploit
│   ├── terrapin_exploit.py     # Main attack script
│   ├── packet_interceptor.py   # Packet manipulation
│   └── requirements.txt        # Python dependencies
│
├── vulnerable_server/          # Vulnerable SSH server
│   ├── Dockerfile
│   ├── sshd_config            # Vulnerable SSH config
│   └── setup.sh
│
├── attacker/                   # Attacker machine
│   ├── Dockerfile
│   ├── mitm_setup.sh          # MITM configuration
│   └── tools/
│       ├── analyze_traffic.sh
│       └── capture_ssh.sh
│
├── client/                     # SSH client
│   ├── Dockerfile
│   └── test_client.py
│
└── demo/                       # Demo scripts
    ├── run_attack.sh
    ├── verify_vulnerability.py
    ├── capture_analysis.py
    └── interactive_demo.py
```
